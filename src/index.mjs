/**
 * idchunk — ESM Entry Point
 * Author: Garv Thakral <https://github.com/codebygarv>
 * License: MIT
 */

import idchunk from "./index.js";

export const numeric = idchunk.numeric;
export const alphanumeric = idchunk.alphanumeric;
export const urlSafe = idchunk.urlSafe;
export const hex = idchunk.hex;
export const custom = idchunk.custom;
export const batch = idchunk.batch;
export const alphabets = idchunk.alphabets;

export default idchunk;
